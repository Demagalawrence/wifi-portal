# Base URL of the Django REST API (must include the trailing /api)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Optional: public site URL used for SEO metadata, sitemap and canonical tags
NEXT_PUBLIC_SITE_URL=http://localhost:3000
